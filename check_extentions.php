$extentions[0]['check']="gd";
$extentions[0]['name']="gd (php graphic ectentions)";
$extentions[1]['check']="curl";
$extentions[1]['name']="curl (php curl ectention)";
	foreach($extentions as $extention){
		if (!extension_loaded('gd')) {
	    }

	}//endif

}// end foreach

